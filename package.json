{
  "name": "serverless-stack-policy-by-resource-type",
  "version": "0.2.0",
  "description": "Serverless Framework plugin for automatically populating CloudFormation stack policy statements by resource type.",
  "main": "src/index.js",
  "scripts": {
    "deploy": "publish",
    "lint": "eslint --ignore-pattern .gitignore **/*.js",
    "test": "nyc --reporter=html --reporter=text mocha test/**/*.js",
    "test:coverage": "nyc report --reporter=text-lcov | coveralls",
    "posttest": "npm run lint"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/neverendingqs/serverless-stack-policy-by-resource-type.git"
  },
  "keywords": [
    "serverless-framework",
    "serverless-framework-plugin",
    "aws",
    "cloudformation"
  ],
  "author": "neverendingqs",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/neverendingqs/serverless-stack-policy-by-resource-type/issues"
  },
  "homepage": "https://github.com/neverendingqs/serverless-stack-policy-by-resource-type#readme",
  "devDependencies": {
    "chai": "^4.2.0",
    "coveralls": "^3.1.0",
    "eslint": "^7.14.0",
    "eslint-plugin-mocha": "^8.0.0",
    "mocha": "^8.2.1",
    "nyc": "^15.1.0",
    "publish-me-maybe": "^1.0.7",
    "sinon": "^9.2.1",
    "sinon-chai": "^3.5.0"
  }
}
